export enum Sort {
  Desc = 'DESC',
  Asc = 'ASC',
}

export enum AppEnum {
  local = 'local',
}

export enum RoleEnum {
  instructor = 'instructor',
  student = 'student',
  admin = 'admin',
}

export enum DayOfWeek {
  monday = 1,
  tuesday = 2,
  wednesday = 3,
  thursday = 4,
  friday = 5,
  saturday = 6,
  sunday = 7,
}
